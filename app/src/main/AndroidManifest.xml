<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.danielstone.materialaboutlibrarydemo"
          xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".Application"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_descriptor"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".ExampleMaterialAboutActivity"
            android:theme="@style/AppTheme.MaterialAboutActivity.Light.DarkActionBar"/>
        <activity
            android:name=".ExampleMaterialAboutFragmentActivity"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity
            android:name=".ExampleMaterialAboutLicenseActivity"
            android:theme="@style/AppTheme.MaterialAboutActivity.Light.DarkActionBar">
        </activity>
    </application>

</manifest>